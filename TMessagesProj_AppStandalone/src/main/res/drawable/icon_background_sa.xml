<?xml version="1.0" encoding="utf-8"?>
<!-- ZG: overrides the library's Telegram-blue gradient with the colour that ships with
     the ZSudo icon pack, so the adaptive mask blends into the artwork at any mask shape. -->
<shape xmlns:android="http://schemas.android.com/apk/res/android">
    <solid android:color="#0B0C10" />
</shape>
